# Signal Schema

Data schemas and type definitions for the Signal application.

## Overview

This directory will contain:
- API schemas
- Data models
- Type definitions
- Validation schemas

## Structure

```
schema/
├── api/          # API request/response schemas
├── models/       # Data model schemas
└── types/        # Shared type definitions
```

## Usage

To be defined as schema requirements are identified.
