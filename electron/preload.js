// Preload script runs in an isolated context with access to Node.js APIs.
// Keep this minimal — only expose what the renderer strictly needs.
// Currently no APIs are exposed; contextBridge can be used here if needed.
